# Obect oriented programming:
class Student:
    name = "Bob"
    age = 30


student1 = Student()
print(student1.name)

# Tasks:

# Add several files to the local repository, create a commit, send it to the remote repository (Push).

# Make changes of all types to the local repository:

# adding a new file;

# modifying an existing file;

# deleting an existing file.

# Create a commit and send it to a remote repository (Push).

# Use GitHub (in the browser) to view the history of commits and the details of the latest commit.
